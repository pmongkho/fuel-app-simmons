{
  "version": 2,
  "builds": [
    {
      "src": "angular-client/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/angular-client"
      }
    }
  ],
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
